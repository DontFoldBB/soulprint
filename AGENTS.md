# Soulprint — agent instructions

**The single source of truth for this project is [`CLAUDE.md`](./CLAUDE.md) (repo root). Read it first.**

This file previously duplicated the project context and drifted out of date (it still
referenced an old toolchain, "no GitHub remote yet", and a stale test count). To avoid two
copies disagreeing, all project context now lives only in `CLAUDE.md`.

See also:
- `docs/handoff.md` — live snapshot when resuming a session.
- `docs/reference/somnia-agents-guide.md` — Somnia Agents + Reactivity reference.
